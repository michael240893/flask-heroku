# Rest Service created with Flask

## `phyton wsgi.py`
This command starts a develoment server. The port defaults to 5000. It is important that dependencies according to requirements.txt are installed.
